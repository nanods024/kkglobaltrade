# ---- Server ----
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173

# ---- MongoDB ----
# Local dev fallback (requires local mongod running):
MONGO_URI=mongodb+srv://nanodigitalservices024_db_user:NMFedbhgHQA3maR9@cluster0.3epupyv.mongodb.net/?appName=Cluster0
# Production (MongoDB Atlas) example:
# MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/kkglobaltrade?retryWrites=true&w=majority

# ---- JWT ----
JWT_SECRET=KkGlobalTrade_2026_SuperSecret_Key_9xP2mL7QaR
JWT_EXPIRES_IN=7d

# ---- Admin seed account (used only by seed script) ----
ADMIN_NAME=KK Global Trade Admin
ADMIN_EMAIL=admin@kkglobaltrade.com
ADMIN_PASSWORD=ChangeMe123!

# ---- Cloudinary (optional) ----
# If these are left blank, the server automatically falls back to storing
# uploaded images locally under /server/uploads and serving them at /uploads.
# Fill these in and set USE_CLOUDINARY=true to switch to Cloudinary storage.
USE_CLOUDINARY=false
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ---- Email (Nodemailer) ----
# If left blank, the server logs enquiry emails to the console instead of
# sending them, so RFQ submissions still work end-to-end during development.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=
EMAIL_PASSWORD=
COMPANY_NOTIFY_EMAIL=kkglobaltrade1@gmail.com

# ---- Rate limiting ----
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX_REQUESTS=200
